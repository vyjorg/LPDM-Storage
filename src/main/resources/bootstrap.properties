#----------------------------------------------------
# THIS FILE IS LOADED BEFORE APPLICATION.PROPERTIES #
#----------------------------------------------------

# APPLICATION
#-----------------------------------------------------
# Set the application name
# (needed for the remote properties)
spring.application.name=ms-storage


# CLOUD
#-----------------------------------------------------
# Allow to override remote properties
spring.cloud.config.allowOverride=true
# Set the external configuration URI
spring.cloud.config.uri=https://config.lpdm.kybox.fr

spring.jpa.hibernate.ddl-auto=validate
spring.thymeleaf.cache=false