version: '3.5'
services:

  # MICROSERVICE CONTAINER
  storage-ms:
    container_name: LPDM-StorageMS
    volumes:
      - './files/:/files'
    build:
      context: .
      dockerfile: Dockerfile
    external_links:
    - LPDM-StorageDB
    ports:
    - '28089:28089'
    restart: always